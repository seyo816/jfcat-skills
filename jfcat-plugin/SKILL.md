---
name: jfcat-plugin
description: >-
  【预留】加菲猫浏览器扩展（jfcat plugin）：Manifest、content/、与页面交互等。具体流程与路径待本目录
  补充文档与脚本后启用；在此之前请勿编造不存在的 API 或文件路径。
metadata:
  openclaw:
    emoji: 🧩
---

# jfcat-plugin（预留）

## 状态

本 skill 为 **功能占位**：用于将来集中描述 **jfcat 浏览器扩展**（仓库内 `content/`、`release/`、`manifest` 等）的开发、调试与页面侧约定。

当前 **无独立 CLI**；与扩展联动的 HTTP/MCP 能力以 **`openclaw-skill-extension-registry`** 及 jfcat-api 文档为准。

## 后续可补充（待办）

- 扩展目录结构、构建与加载方式（简要）
- 与 `jfcat-cli` 本地调试浏览器的配合说明
- 与 `ExtensionPresenceService` / 插件心跳相关的 Agent 提示（若需要）

人类说明见仓库 **`skills/README.md`**。
